openapi: 3.0.1
info:
  title: iterate_api
  description: API Definition of iterate_api
  version: 1.0.0
servers:
- url: http://localhost:8290/iterate
paths:
  /:
    post:
      responses:
        default:
          description: Default response
